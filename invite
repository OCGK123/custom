<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CustomBot - 초대</title>
    <style>
        /* 폰트 설정 */
        @font-face {
            font-family: 'PyeongChangPeace-Bold';
            src: url('https://fastly.jsdelivr.net/gh/projectnoonnu/noonfonts_2206-02@1.0/PyeongChangPeace-Bold.woff2') format('woff2');
            font-weight: 700;
            font-style: normal;
        }
        
        @font-face {
            font-family: 'SBAggroB';
            src: url('https://fastly.jsdelivr.net/gh/projectnoonnu/noonfonts_2108@1.1/SBAggroB.woff') format('woff');
            font-weight: normal;
            font-style: normal;
        }
        
        :root {
            --primary: #5865F2;
            --primary-dark: #4752c4;
            --secondary: #EB459E;
            --dark: #2C2F33;
            --darker: #23272A;
            --light: #FFFFFF;
            --light-gray: #F6F6F6;
            --success: #57F287;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        html {
            scroll-behavior: smooth;
        }
        
        body {
            font-family: 'SBAggroB', sans-serif;
            background-color: var(--dark);
            color: var(--light);
            overflow-x: hidden;
            height: 100vh;
            display: flex;
            flex-direction: column;
        }
        
        h1, h2, h3, h4, h5, h6 {
            font-family: 'PyeongChangPeace-Bold', sans-serif;
            margin-bottom: 1rem;
        }
        
        p {
            line-height: 1.6;
            margin-bottom: 1rem;
        }
        
        a {
            text-decoration: none;
            color: var(--primary);
            transition: color 0.3s ease;
        }
        
        a:hover {
            color: var(--secondary);
        }
        
        .container {
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
        }
        
        /* 헤더 */
        header {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            z-index: 1000;
            background-color: rgba(35, 39, 42, 0.8);
            backdrop-filter: blur(10px);
            transition: all 0.3s ease;
        }
        
        header.scrolled {
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.3);
        }
        
        .header-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 2rem;
        }
        
        .logo {
            display: flex;
            align-items: center;
            gap: 1rem;
        }
        
        .logo img {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            transition: transform 0.3s ease;
        }
        
        .logo:hover img {
            transform: rotate(10deg);
        }
        
        .logo h1 {
            font-size: 1.5rem;
            margin: 0;
            background: linear-gradient(to right, var(--primary), var(--secondary));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }
        
        nav ul {
            display: flex;
            list-style: none;
            gap: 2rem;
        }
        
        nav a {
            color: var(--light);
            font-weight: bold;
            position: relative;
            padding: 0.5rem 0;
        }
        
        nav a::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 0;
            height: 2px;
            background: linear-gradient(to right, var(--primary), var(--secondary));
            transition: width 0.3s ease;
        }
        
        nav a:hover::after {
            width: 100%;
        }
        
        .burger {
            display: none;
            cursor: pointer;
        }
        
        .burger div {
            width: 25px;
            height: 3px;
            background-color: var(--light);
            margin: 5px;
            transition: all 0.3s ease;
        }
        
        /* 메인 콘텐츠 */
        main {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 80px 0;
            position: relative;
            overflow: hidden;
        }
        
        /* 배경 효과 */
        .background {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
        }
        
        .gradient-bg {
            background: linear-gradient(135deg, var(--dark) 0%, var(--darker) 100%);
            width: 100%;
            height: 100%;
            position: absolute;
        }
        
        .particles {
            width: 100%;
            height: 100%;
            position: absolute;
            opacity: 0.5;
        }
        
        .pulse-circle {
            position: absolute;
            border-radius: 50%;
            opacity: 0;
            transform: scale(0);
            background: radial-gradient(circle, rgba(88, 101, 242, 0.3) 0%, rgba(88, 101, 242, 0) 70%);
            animation: pulse 8s infinite ease-out;
        }
        
        .pulse-circle:nth-child(1) {
            width: 600px;
            height: 600px;
            top: 20%;
            left: 30%;
            animation-delay: 0s;
        }
        
        .pulse-circle:nth-child(2) {
            width: 800px;
            height: 800px;
            top: 40%;
            left: 60%;
            animation-delay: 2s;
        }
        
        .pulse-circle:nth-child(3) {
            width: 500px;
            height: 500px;
            top: 70%;
            left: 20%;
            animation-delay: 4s;
            background: radial-gradient(circle, rgba(235, 69, 158, 0.3) 0%, rgba(235, 69, 158, 0) 70%);
        }
        
        .pulse-circle:nth-child(4) {
            width: 700px;
            height: 700px;
            top: 10%;
            left: 70%;
            animation-delay: 6s;
            background: radial-gradient(circle, rgba(235, 69, 158, 0.2) 0%, rgba(235, 69, 158, 0) 70%);
        }
        
        .geometric-shapes {
            position: absolute;
            width: 100%;
            height: 100%;
            overflow: hidden;
            opacity: 0.15;
        }
        
        .shape {
            position: absolute;
            border-radius: 50%;
            background: linear-gradient(to right, var(--primary), var(--secondary));
            opacity: 0.5;
            filter: blur(50px);
        }
        
        .shape:nth-child(1) {
            width: 300px;
            height: 300px;
            top: -100px;
            left: -100px;
        }
        
        .shape:nth-child(2) {
            width: 200px;
            height: 200px;
            bottom: -50px;
            right: -50px;
        }
        
        .shape:nth-child(3) {
            width: 150px;
            height: 150px;
            top: 40%;
            right: 20%;
        }
        
        .grid-lines {
            position: absolute;
            width: 100%;
            height: 100%;
            background-image: linear-gradient(to right, rgba(255, 255, 255, 0.05) 1px, transparent 1px),
                              linear-gradient(to bottom, rgba(255, 255, 255, 0.05) 1px, transparent 1px);
            background-size: 50px 50px;
            opacity: 0.4;
        }
        
        /* 초대 카드 */
        .invite-card {
            position: relative;
            background: rgba(35, 39, 42, 0.7);
            border-radius: 20px;
            padding: 40px;
            max-width: 600px;
            width: 100%;
            text-align: center;
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            z-index: 10;
            transform: translateY(50px);
            opacity: 0;
            animation: fadeUp 1s forwards 0.5s;
        }
        
        .invite-card-header {
            margin-bottom: 30px;
        }
        
        .invite-card-header h2 {
            font-size: 2.5rem;
            margin-bottom: 0.5rem;
            background: linear-gradient(to right, var(--primary), var(--secondary));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }
        
        .invite-card-header p {
            font-size: 1.2rem;
            color: rgba(255, 255, 255, 0.8);
        }
        
        .invite-logo {
            width: 120px;
            height: 120px;
            margin: 0 auto 30px;
            border-radius: 50%;
            padding: 15px;
            background: rgba(255, 255, 255, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            animation: floating 3s ease-in-out infinite;
        }
        
        .invite-logo img {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            object-fit: cover;
        }
        
        .invite-features {
            display: flex;
            flex-wrap: wrap;
            gap: 15px;
            margin-bottom: 30px;
            justify-content: center;
        }
        
        .feature-tag {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 30px;
            padding: 8px 16px;
            font-size: 0.9rem;
            color: rgba(255, 255, 255, 0.9);
            border: 1px solid rgba(255, 255, 255, 0.1);
            transition: all 0.3s ease;
            transform: scale(0.9);
            opacity: 0;
            animation: popIn 0.5s forwards;
        }
        
        .feature-tag:nth-child(1) {
            animation-delay: 0.7s;
        }
        
        .feature-tag:nth-child(2) {
            animation-delay: 0.8s;
        }
        
        .feature-tag:nth-child(3) {
            animation-delay: 0.9s;
        }
        
        .feature-tag:nth-child(4) {
            animation-delay: 1s;
        }
        
        .feature-tag:nth-child(5) {
            animation-delay: 1.1s;
        }
        
        .feature-tag:hover {
            background: rgba(255, 255, 255, 0.2);
            transform: translateY(-3px);
        }
        
        .feature-tag i {
            margin-right: 5px;
            color: var(--primary);
        }
        
        .invite-btn {
            display: inline-block;
            padding: 15px 40px;
            background: linear-gradient(to right, var(--primary), var(--secondary));
            color: var(--light);
            border-radius: 50px;
            font-size: 1.2rem;
            font-weight: bold;
            margin-bottom: 20px;
            box-shadow: 0 10px 30px rgba(88, 101, 242, 0.5);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            transform: scale(0.9);
            opacity: 0;
            animation: popIn 0.5s forwards 1.2s;
        }
        
        .invite-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(to right, rgba(255, 255, 255, 0), rgba(255, 255, 255, 0.3), rgba(255, 255, 255, 0));
            transform: translateX(-100%);
            transition: transform 1s ease;
        }
        
        .invite-btn:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 40px rgba(88, 101, 242, 0.7);
            color: var(--light);
        }
        
        .invite-btn:hover::before {
            transform: translateX(100%);
        }
        
        .invite-btn i {
            margin-right: 10px;
        }
        
        .servers-count {
            font-size: 0.95rem;
            color: rgba(255, 255, 255, 0.7);
            margin-bottom: 20px;
            transform: scale(0.9);
            opacity: 0;
            animation: popIn 0.5s forwards 1.3s;
        }
        
        .servers-count span {
            color: var(--light);
            font-weight: bold;
        }
        
        .card-footer {
            font-size: 0.9rem;
            color: rgba(255, 255, 255, 0.6);
            opacity: 0;
            animation: fadeIn 1s forwards 1.4s;
        }
        
        .card-footer a {
            color: rgba(255, 255, 255, 0.8);
            text-decoration: underline;
            transition: all 0.3s ease;
        }
        
        .card-footer a:hover {
            color: var(--light);
        }
        
        /* 인증 배지 */
        .verification-badge {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            background: rgba(87, 242, 135, 0.15);
            border-radius: 20px;
            padding: 5px 12px;
            font-size: 0.85rem;
            color: var(--success);
            margin-top: 20px;
            border: 1px solid rgba(87, 242, 135, 0.3);
            transform: scale(0.9);
            opacity: 0;
            animation: popIn 0.5s forwards 1.5s;
        }
        
        .verification-badge i {
            font-size: 0.8rem;
        }
        
        /* 추가 링크 */
        .additional-links {
            display: flex;
            justify-content: center;
            gap: 30px;
            margin-top: 30px;
            flex-wrap: wrap;
        }
        
        .link-item {
            display: flex;
            align-items: center;
            gap: 8px;
            color: rgba(255, 255, 255, 0.7);
            font-size: 0.9rem;
            transition: all 0.3s ease;
            opacity: 0;
            animation: fadeIn 0.5s forwards 1.6s;
        }
        
        .link-item:nth-child(2) {
            animation-delay: 1.7s;
        }
        
        .link-item:nth-child(3) {
            animation-delay: 1.8s;
        }
        
        .link-item:hover {
            color: var(--light);
        }
        
        .link-item i {
            font-size: 1rem;
        }
        
        /* 파티클 효과 */
        .particle {
            position: absolute;
            width: 5px;
            height: 5px;
            border-radius: 50%;
            background-color: rgba(255, 255, 255, 0.5);
            pointer-events: none;
            opacity: 0;
        }
        
        /* 푸터 */
        footer {
            background-color: var(--darker);
            padding: 20px 0;
            text-align: center;
            position: relative;
            z-index: 10;
        }
        
        .footer-content {
            color: rgba(255, 255, 255, 0.7);
            font-size: 0.9rem;
        }
        
        .footer-content a {
            color: rgba(255, 255, 255, 0.8);
            transition: all 0.3s ease;
        }
        
        .footer-content a:hover {
            color: var(--light);
        }
        
        /* 애니메이션 */
        @keyframes fadeUp {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        @keyframes fadeIn {
            to {
                opacity: 1;
            }
        }
        
        @keyframes popIn {
            to {
                opacity: 1;
                transform: scale(1);
            }
        }
        
        @keyframes floating {
            0% {
                transform: translateY(0px);
            }
            50% {
                transform: translateY(-10px);
            }
            100% {
                transform: translateY(0px);
            }
        }
        
        @keyframes pulse {
            0% {
                transform: scale(0);
                opacity: 0.5;
            }
            50% {
                opacity: 0.2;
            }
            100% {
                transform: scale(1.5);
                opacity: 0;
            }
        }
        
        /* 반응형 디자인 */
        @media screen and (max-width: 992px) {
            .invite-card {
                max-width: 90%;
                padding: 30px;
            }
            
            .invite-card-header h2 {
                font-size: 2.2rem;
            }
        }
        
        @media screen and (max-width: 768px) {
            .header-container {
                padding: 1rem;
            }
            
            .nav-links {
                position: fixed;
                top: 70px;
                right: -100%;
                flex-direction: column;
                background-color: var(--darker);
                width: 80%;
                height: 100vh;
                padding: 2rem;
                z-index: 100;
                box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
                transition: right 0.5s ease;
            }
            
            .nav-links.active {
                right: 0;
            }
            
            .burger {
                display: block;
            }
            
            .burger.active div:nth-child(1) {
                transform: rotate(-45deg) translate(-5px, 6px);
            }
            
            .burger.active div:nth-child(2) {
                opacity: 0;
            }
            
            .burger.active div:nth-child(3) {
                transform: rotate(45deg) translate(-5px, -6px);
            }
            
            .invite-card-header h2 {
                font-size: 1.8rem;
            }
            
            .invite-card-header p {
                font-size: 1rem;
            }
            
            .invite-logo {
                width: 100px;
                height: 100px;
            }
            
            .invite-btn {
                padding: 12px 30px;
                font-size: 1.1rem;
            }
        }
        
        @media screen and (max-width: 576px) {
            .invite-card {
                padding: 25px 15px;
            }
            
            .invite-features {
                gap: 10px;
            }
            
            .feature-tag {
                font-size: 0.8rem;
                padding: 6px 12px;
            }
            
            .invite-btn {
                padding: 12px 25px;
                font-size: 1rem;
                width: 100%;
            }
            
            .additional-links {
                flex-direction: column;
                gap: 15px;
                align-items: center;
            }
        }
    </style>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <!-- 헤더 -->
    <header>
        <div class="header-container">
            <a href="http://custombot.kro.kr" class="logo">
                <h1>커스텀봇</h1>
            </a>
            <nav>
                <ul class="nav-links">
                    <li><a href="http://custombot.kro.kr#features">기능</a></li>
                    <li><a href="http://custombot.kro.kr#commands">명령어</a></li>
                    <li><a href="http://custombot.kro.kr#how-to-use">사용 방법</a></li>
                    <li><a href="http://custombot.kro.kr/support">문의하기</a></li>
                </ul>
            </nav>
            <div class="burger">
                <div></div>
                <div></div>
                <div></div>
            </div>
        </div>
    </header>

    <!-- 메인 콘텐츠 -->
    <main>
        <!-- 배경 효과 -->
        <div class="background">
            <div class="gradient-bg"></div>
            <div class="particles" id="particles-js"></div>
            <div class="geometric-shapes">
                <div class="shape"></div>
                <div class="shape"></div>
                <div class="shape"></div>
            </div>
            <div class="grid-lines"></div>
            <div class="pulse-circle"></div>
            <div class="pulse-circle"></div>
            <div class="pulse-circle"></div>
            <div class="pulse-circle"></div>
        </div>
        
        <!-- 초대 카드 -->
        <div class="invite-card">

            
            <div class="invite-card-header">
                <h2>디스코드에 추가하기</h2>
                <p>서버에 커스텀봇을 초대하고 커스텀 명령어를 사용해보세요!</p>
            </div>
            
            <div class="invite-features">
                <div class="feature-tag"><i class="fas fa-comment"></i> 텍스트 명령어</div>
                <div class="feature-tag"><i class="fas fa-user-tag"></i> 역할 명령어</div>
                <div class="feature-tag"><i class="fas fa-palette"></i> 임베드 명령어</div>
                <div class="feature-tag"><i class="fas fa-random"></i> 랜덤 응답</div>
                <div class="feature-tag"><i class="fas fa-calculator"></i> 카운터 시스템</div>
            </div>
            
            <a href="#" class="invite-btn" id="inviteBtn">
                <i class="fas fa-plus-circle"></i>
                서버에 추가하기
            </a>
            
            <div class="servers-count">
                현재 <span class="counter" data-target="6">0</span>개의 서버에서 사용 중
            </div>
            
            <div class="verification-badge">
                <i class="fas fa-shield-alt"></i>
                Discord 인증 완료
            </div>
            
            <div class="additional-links">
                <a href="http://custombot.kro.kr" class="link-item">
                    <i class="fas fa-home"></i>
                    홈페이지
                </a>
                <a href="http://support.custombot.kro.kr" class="link-item">
                    <i class="fas fa-question-circle"></i>
                    도움말
                </a>
                <a href="#" class="link-item">
                    <i class="fab fa-discord"></i>
                    지원 서버
                </a>
            </div>
            
            <div class="card-footer">
                초대함으로써 <a href="http://custombot.kro.kr/tos">서비스 약관</a>과 <a href="http://custombot.kro.kr/privacy-policy">개인정보 처리방침</a>에 동의합니다.
            </div>
        </div>
    </main>

    <!-- 푸터 -->
    <footer>
        <div class="container">
            <div class="footer-content">
                <p><a href="http://custombot.kro.kr">홈페이지</a> | <a href="http://support.custombot.kro.kr">고객 지원</a></p>
            </div>
        </div>
    </footer>

    <!-- 자바스크립트 -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/particles.js/2.0.0/particles.min.js"></script>
    <script>
        // 헤더 스크롤 효과
        window.addEventListener('scroll', function() {
            let header = document.querySelector('header');
            
            if (window.scrollY > 20) {
                header.classList.add('scrolled');
            } else {
                header.classList.remove('scrolled');
            }
        });
        
        // 모바일 메뉴
        const burger = document.querySelector('.burger');
        const navLinks = document.querySelector('.nav-links');
        
        burger.addEventListener('click', () => {
            burger.classList.toggle('active');
            navLinks.classList.toggle('active');
        });
        
        // 초대 버튼 클릭
        document.getElementById('inviteBtn').addEventListener('click', function(e) {
            e.preventDefault();
            
            // 애니메이션 효과
            createParticles(e);
            
            // 잠시 후 디스코드 초대 링크로 이동
            setTimeout(() => {
                // 실제 디스코드 봇 초대 링크로 대체해야 합니다
                window.location.href = 'https://discord.com/api/oauth2/authorize?client_id=1347619570596184146&permissions=8&scope=bot';
            }, 500);
        });
        
        // 파티클 효과 생성
        function createParticles(e) {
            const x = e.clientX;
            const y = e.clientY;
            const button = e.target.closest('.invite-btn');
            const buttonRect = button.getBoundingClientRect();
            const buttonCenterX = buttonRect.left + buttonRect.width / 2;
            const buttonCenterY = buttonRect.top + buttonRect.height / 2;
            
            for (let i = 0; i < 30; i++) {
                createParticle(buttonCenterX, buttonCenterY);
            }
        }
        
        function createParticle(x, y) {
            const particle = document.createElement('div');
            particle.classList.add('particle');
            document.body.appendChild(particle);
            
            // 초기 위치
            particle.style.left = `${x}px`;
            particle.style.top = `${y}px`;
            
            // 랜덤 크기
            const size = Math.random() * 5 + 5;
            particle.style.width = `${size}px`;
            particle.style.height = `${size}px`;
            
            // 랜덤 색상
            const colors = ['#5865F2', '#EB459E', '#57F287', '#FEE75C', '#FFFFFF'];
            particle.style.backgroundColor = colors[Math.floor(Math.random() * colors.length)];
            
            // 애니메이션
            const angle = Math.random() * Math.PI * 2;
            const speed = Math.random() * 100 + 50;
            const vx = Math.cos(angle) * speed;
            const vy = Math.sin(angle) * speed;
            
            // 시작 애니메이션
            particle.style.opacity = '0.8';
            
            // 타임라인 애니메이션
            const startTime = performance.now();
            const duration = Math.random() * 1000 + 500;
            
            function animate() {
                const elapsed = performance.now() - startTime;
                const progress = Math.min(elapsed / duration, 1);
                
                particle.style.transform = `translate(${vx * progress}px, ${vy * progress}px)`;
                particle.style.opacity = (1 - progress).toString();
                
                if (progress < 1) {
                    requestAnimationFrame(animate);
                } else {
                    particle.remove();
                }
            }
            
            requestAnimationFrame(animate);
        }
        
        // 카운터 애니메이션
        const counters = document.querySelectorAll('.counter');
        
        function runCounter() {
            counters.forEach(counter => {
                const target = +counter.getAttribute('data-target');
                const count = +counter.innerText;
                
                const inc = target / 100;
                
                if (count < target) {
                    counter.innerText = Math.ceil(count + inc);
                    setTimeout(runCounter, 20);
                } else {
                    counter.innerText = target.toLocaleString();
                }
            });
        }
        
        // 페이지 로드 후 애니메이션 시작
        document.addEventListener('DOMContentLoaded', () => {
            setTimeout(runCounter, 1500);
            
            // 파티클 배경
            particlesJS("particles-js", {
                "particles": {
                    "number": {
                        "value": 80,
                        "density": {
                            "enable": true,
                            "value_area": 800
                        }
                    },
                    "color": {
                        "value": "#ffffff"
                    },
                    "shape": {
                        "type": "circle",
                        "stroke": {
                            "width": 0,
                            "color": "#000000"
                        },
                        "polygon": {
                            "nb_sides": 5
                        }
                    },
                    "opacity": {
                        "value": 0.3,
                        "random": true,
                        "anim": {
                            "enable": false,
                            "speed": 1,
                            "opacity_min": 0.1,
                            "sync": false
                        }
                    },
                    "size": {
                        "value": 3,
                        "random": true,
                        "anim": {
                            "enable": false,
                            "speed": 40,
                            "size_min": 0.1,
                            "sync": false
                        }
                    },
                    "line_linked": {
                        "enable": true,
                        "distance": 150,
                        "color": "#ffffff",
                        "opacity": 0.2,
                        "width": 1
                    },
                    "move": {
                        "enable": true,
                        "speed": 1,
                        "direction": "none",
                        "random": false,
                        "straight": false,
                        "out_mode": "out",
                        "bounce": false,
                        "attract": {
                            "enable": false,
                            "rotateX": 600,
                            "rotateY": 1200
                        }
                    }
                },
                "interactivity": {
                    "detect_on": "canvas",
                    "events": {
                        "onhover": {
                            "enable": true,
                            "mode": "grab"
                        },
                        "onclick": {
                            "enable": true,
                            "mode": "push"
                        },
                        "resize": true
                    },
                    "modes": {
                        "grab": {
                            "distance": 140,
                            "line_linked": {
                                "opacity": 0.5
                            }
                        },
                        "bubble": {
                            "distance": 400,
                            "size": 40,
                            "duration": 2,
                            "opacity": 8,
                            "speed": 3
                        },
                        "repulse": {
                            "distance": 200,
                            "duration": 0.4
                        },
                        "push": {
                            "particles_nb": 4
                        },
                        "remove": {
                            "particles_nb": 2
                        }
                    }
                },
                "retina_detect": true
            });
        });
    </script>
</body>
</html>
